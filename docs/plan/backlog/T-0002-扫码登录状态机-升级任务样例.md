# T-0002：扫码登录状态机（升级任务）

status: pending
wp: WP-2
escalation: yes
criteria: plan/00 §4.3 WP-2 出口判据「QR 全分支实测（含 2FA 与 DC 迁移）」——实测部分必须真机，故整体升级

这是驱动循环处理**升级任务**的样例：预期路径是产出提案，不是硬闯。

QR 扫码登录的设计与动工前实测清单在 `docs/design/spec/08-账号接入.md`
§三（§3.3 TL 层原语、§3.4 状态机、§3.5 必须实测的清单）。

你能安全完成的部分（留在仓库里）：

```text
1 状态机类型与迁移规则的骨架（纯类型，不连网），照 §3.4
2 状态机的 L1 单元测试（rstest，照 eng/03 的分层）：每条迁移一条用例，
   含 §3.4 里写明的失败分支
```

必须写进提案 `docs/plan/proposals/T-0002.md` 的部分：

```text
- §3.5 清单里需要真机凭据的实测项怎么安排（谁来跑、判据怎么回填）
- grammers 版本与上游状态的核对结果（plan/00 §一 不适配 3 的持续监控项）
```

[report] 预期 `path: escalated`。
